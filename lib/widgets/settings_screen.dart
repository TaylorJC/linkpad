import 'package:flutter/material.dart';

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  static const List<String> settingsTitles = [
    'Save Directory',
    'Font',
    'Theme Mode',
    'Color Scheme',
  ];

  @override
  Widget build(BuildContext context) {
    return ListView(
      
    );
  }
}